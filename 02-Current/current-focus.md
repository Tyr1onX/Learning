---
tags:
  - current
  - learning
status: active
updated: 2026-09-06
---

# 当前学习焦点

## 当前总目标

面向下一次字节跳动日常实习 / 暑期实习，按“每天一个明确学习主题 + 一道 Hot 100 算法任务”的固定节奏推进全栈能力。当天任务完成后停止继续备战，避免无休止加量。

Learning 仓库是跨窗口唯一进度来源；所有掌握状态、复习节点和下一断点都以仓库实际记录为准。

## 已完成主干

Web / Network / Browser 已完成一轮系统学习并经过无提示混合回忆，主干维持 `understood`。详见 `03-Knowledge/Web/`。

## 当前全栈学习主线

### JavaScript 基础第一轮进度

2026-08-27：变量、基础类型、`undefined` / `null`、`typeof`、`==` / `===`。

2026-08-28：对象 / 数组、属性访问、共享引用直觉、`const` 与对象内部可变性。

2026-08-29：函数定义与调用、形参与实参、`return`、无显式 `return` 时的 `undefined`、局部变量与基础作用域方向。

2026-08-30：词法作用域、块级作用域、shadowing、`return fn` vs `return fn()`、闭包基础。

2026-08-31：`this` 基础：普通函数 `this` 主要看调用方式；方法取出独立调用会丢失原对象关系；普通内部函数不自动继承外层 `this`；箭头函数使用外层词法环境中的 `this`。

2026-09-01：`prototype` / 原型基础：自身属性优先，缺失时沿原型链查找；`Object.create(proto)` 建立原型关系；自身同名属性遮蔽原型属性；实例数据与 `prototype` 上共享方法分开理解。

2026-09-02：DOM / Event 基础：

- `document` 是访问当前页面 DOM 的入口；
- `document.querySelector("#id")` 返回匹配的 DOM 元素对象；
- `textContent` 可读取 / 修改元素文本；
- `addEventListener("click", handler)` 注册事件处理函数，事件发生时再执行；
- `event.target` 表示实际触发事件的元素；
- 当前易错点是 DOM API 大小写、选择器字符串引号，以及从零恢复固定语法。

2026-09-06：Promise / async-await / Event Loop 基础：

- 当前同步代码先执行；
- `Promise.then` 与 `await` 后续属于微任务；
- 当前同步代码结束后先清空微任务，再处理 `setTimeout` 等后续任务；
- `async` 函数一定返回 Promise；
- 调用 `async` 函数时会先同步执行到第一个 `await`；
- `await` 只暂停当前 async 函数后续执行，不阻塞整个 JavaScript；
- 当前易错点：综合顺序题中曾混淆 `await` 前同步执行与 `await` 后微任务，需继续间隔复习。

这些内容仍处于第一轮 + 间隔复习阶段，不提前整体标记为 `understood`。

### 基础知识间隔复习

基础知识与算法一样必须通过无提示回忆验证，不允许只学习不复习。

执行规则：

- 每天最多安排一个旧基础知识主题；
- 多个节点同时到期时优先最早逾期项，其余顺延；
- 必须根据用户实际回答记录通过 / 易错 / 待复习；
- 晚间复习必须先提问、等待真实回答，再回写；
- 每个知识主题至少保留 D+7 与 D+21 长期验证节点；出现错误时追加更近节点。

当前队列：

- 变量 / 基础类型：D+7 已于 2026-09-06 逾期补复习通过；D+21：2026-09-17；
- 对象 / 数组：D+7 原定 2026-09-04，当前逾期；D+21：2026-09-18；
- 函数定义与调用：D+7 原定 2026-09-05，当前逾期；D+21：2026-09-19；
- 词法作用域 / 闭包：D+7 原定 2026-09-06，当前待补；D+21：2026-09-20；
- `this`：D+7：2026-09-07；D+21：2026-09-21；
- `prototype`：D+7：2026-09-08；D+21：2026-09-22；
- DOM / Event：D+7：2026-09-09；D+21：2026-09-23；
- Promise / async-await / Event Loop：D+1：2026-09-07；D+3：2026-09-09；D+7：2026-09-13；D+21：2026-09-27。

### 当前下一学习断点

下一个单日主题候选顺序：

```text
fetch / JSON / API
→ TypeScript
→ React
→ Node.js Backend
→ SQL / Database
→ Auth / Full-stack interaction
→ Linux / Docker / Deployment / CI
```

每天只推进一个明确主题，不一次跨越多个主题。

## 算法主线

### 已完成第一轮 / 当前状态

- 2026-08-27：LeetCode 49「字母异位词分组」；
- 2026-08-28：LeetCode 128「最长连续序列」；
- 2026-08-29：LeetCode 283「移动零」；
- 2026-08-30：LeetCode 11「盛最多水的容器」；
- 2026-08-31：LeetCode 15「三数之和」；
- 2026-09-01：LeetCode 42「接雨水」；
- 2026-09-02：LeetCode 438「找到字符串中所有字母异位词」；
- 2026-09-06：LeetCode 560「和为 K 的子数组」前缀和 + 哈希表实现已完成。

### 当前需要稳定的算法 / C++ 点

- `unordered_map<Key, Value>` 与 `unordered_set` 的语义区别；
- 写哈希表前先明确 key / value 的业务含义；
- `count(key)` 主要判断 key 是否存在，不等于业务上的出现次数；
- 局部基本类型变量不会自动初始化为 0；
- 复杂度分析时区分输入空间、辅助空间、输出空间；
- 49：排序后的字符串作为 key；
- 128：只有连续序列起点启动内部 `while`，平均 `O(n)`、空间 `O(n)`；
- 283：`write` 表示下一个非零元素写入位置；`O(n)` / `O(1)`；
- 11：移动较高边只会缩小宽度而短板不变，应移动较短边；
- 15：排序 + 固定 `i` + 双指针 + 去重；时间 `O(n^2)`；
- 42：依据较小的 `leftMax/rightMax` 结算对应侧；`O(n)` / `O(1)`；
- 438：固定长度滑动窗口，右进 `+1`、左出 `-1`；`O(n)` / `O(1)`；
- 560：哈希表语义为“前缀和 -> 出现次数”；每轮 `sum += x`，答案加上 `count[sum - k]`，再 `count[sum]++`；`count[0] = 1` 防止漏掉从开头开始的合法子数组；允许负数时窗口和不单调，不能简单按和大小收缩滑动窗口；平均 `O(n)` / `O(n)`。

### 近期复习节点

LeetCode 49：
- D+7 已于 2026-09-06 逾期补复习通过；
- D+21：2026-09-17。

LeetCode 128：
- D+7 已于 2026-09-06 逾期补复习通过；
- D+21：2026-09-18。

LeetCode 283：
- D+7 已于 2026-09-06 逾期补复习通过；
- D+21：2026-09-19。

LeetCode 11：
- D+7：2026-09-06 已通过；
- D+21：2026-09-20。

LeetCode 15：
- D+3 已于 2026-09-06 逾期补复习通过；
- D+7：2026-09-07；
- D+21：2026-09-21。

LeetCode 42：
- D+3 已于 2026-09-06 逾期补复习通过；
- D+7：2026-09-08；
- D+21：2026-09-22。

LeetCode 438：
- D+3 已于 2026-09-06 逾期补复习通过；
- D+7：2026-09-09；
- D+21：2026-09-23。

LeetCode 560：
- D+1：2026-09-07；
- D+3：2026-09-09；
- D+7：2026-09-13；
- D+21：2026-09-27。

## 每日执行规则

每天开始时固定给出：

```text
今日学习主题
完成标准
今日算法新题
需要复习的旧算法（如有）
需要复习的旧基础知识（如有，最多一个主题）
今日停止线
```

算法题开始时固定先给：

```text
题干
输入 / 输出要什么
一个具体例子
再进入思路
```

所有复习必须通过交互式无提示回忆完成；只有用户实际回答后，才能记录通过点、遗忘点和易错点。

每天固定任务完成后立即结束，不追加第二个知识主题或第二道新算法题。重要知识点和算法都通过间隔回忆复习。

## 最近一次会话

[[05-Progress/Sessions/2026-09-06-bytedance-day-8]]
