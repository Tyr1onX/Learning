---
tags:
  - progress
  - python
status: completed
updated: 2026-09-09
---

# 2026-09-09 Python 基础第一轮

## 本次学习

已在 VS Code 中安装 Python、Pylance、Python Debugger 扩展，并确认 Python 可以运行。完成变量与输入、条件判断、for/range、while、列表、enumerate、函数、字典计数与遍历的第一轮练习；集合刚进入示例阶段。

知识笔记：[[03-Knowledge/Python/python-basics]]。

## 实际回答与掌握情况

- 正负零判断：判断逻辑正确，最初漏写 `if` / `elif` / `else` 的冒号。
- `range()`：最初用 `range(10)` 输出 1～10、用 `range(1, 11, 2)` 输出偶数；经纠正理解起点、终点和步长。
- 求和：能够写累加循环，最初漏初始化、漏包含 100、忘记将 `input()` 转为整数。
- `while`：最初漏 `i += 1` 且使用 `< n`；纠正后独立写出从 n 倒数到 1 的正确循环。
- 列表求和、筛选偶数、筛选大于 5 后乘 2：均能独立完成；曾将 `print()` 放在循环内，后明确输出位置差异。
- 最大/最小值：能写遍历比较；已纠正用 0 初始化导致全负数错误，以及把循环变量 `i` 当成目标下标的问题。
- 函数：独立写出 `count_even(nums)` 和 `find_max(nums)`，能正确使用 `return`；删除未使用的 `result = []`。
- 字典：经分步讲解后独立完成新增、更新、计数和 `items()` 遍历；最初误用 `count[0].value`，且只保存最大次数，后独立写出同时保存数字与次数的正确版本。
- 集合：已介绍 `set()`、`add()`、`in`，尚未提交去重题答案。

## 当前易错点

冒号与缩进；`input()` 的字符串类型；`range()` 右边界；`while` 的循环变量更新；`//` 与 `%`；最佳值初始化；值与下标、键与值的区别；`return` 与 `print()`；避免覆盖 `sum` / `min` / `max`。

## 下一断点

2026-09-10 复习上述语法，先做无提示小题。首题可从集合去重开始，再混合检查循环、列表和字典。通过复习后再继续字符串等内容；当前状态保持 `learning`，不标记为 `understood`。
